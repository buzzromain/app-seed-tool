# Review screenshots

Images referenced from a review comment on LedgerHQ/app-seed-tool#14.

They are captured from `tests/functional/` running on Speculos (Stax) against
the `bip85` branch, and they are linked by URL from that comment.

**Please do not delete this branch while that pull request is open.** Deleting it
turns every image in the comment into a broken link. Nothing here is code and
nothing here is meant to be merged.
